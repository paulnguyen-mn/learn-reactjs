import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

Header.propTypes = {
  
};

function Header(props) {
  return (
    <div>
      Header
    </div>
  );
}

export default Header;